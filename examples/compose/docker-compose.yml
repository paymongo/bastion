version: "2"
volumes:
  home:
  etc:
services:
  bastion:
    image: 001299781416.dkr.ecr.ap-southeast-1.amazonaws.com/bastion
    mem_limit: "512000000"
    ports:
      - "22:22"
    env_file:
      - bastion.env
    volumes:
      - home:/home
      - etc:/etc
  gak:
    image: cloudposse/github-authorized-keys
    mem_limit: "128000000"
    ports:
      - "301:301"
    volumes:
      - home:/home
      - etc:/etc
    env_file:
      - gak.env
    links:
      - "etcd:etcd"
    restart: always
  etcd:
    image: quay.io/coreos/etcd:v2.3.7
    mem_limit: "128000000"
    command:
      - "--advertise-client-urls=http://0.0.0.0:2379,http://0.0.0.0:4001"
      - "--listen-client-urls=http://0.0.0.0:2379,http://0.0.0.0:4001"
    ports:
      - "2379:2379"
      - "2380:2380"
      - "4001:4001"
      - "7001:7001"
    restart: always